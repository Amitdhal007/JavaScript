"use strict"; // from now treat all js in newer version

// from now all the code which is going to written will follow modern javascript

let userName = "Amit Kumar Dhal"
let age = 23
let isLoggedIn = false
let kyaKru;
let empty = null

console.log(typeof userName);
console.log(typeof age)
console.log(typeof isLoggedIn)
console.log(typeof kyaKru)

// data types
// number -> 2 ^ 53 Range
// boolean -> false/true
// string -> "hello"
// bigint 
// null -> standalone value (representaion of empty) / object
// undefined -> undefined (value not assigned only variable declared)
// symbols are used to find uniqueness like in react there are multiple components so to find the unique component symbol is used